import 'package:flutter/material.dart';


class PhotoFull extends StatelessWidget {
  const PhotoFull({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}